version: '3.9'

services:
  flask-app-cookies:
    build: ./app
    image: learning_team/flask_app_libraries:latest
    container_name: my-flask-app-libraries
    ports:
      - 5000:5000
    volumes:
      - ./app:/app # Temporal NOT use in production
